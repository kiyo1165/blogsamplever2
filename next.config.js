/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["nextjsblog-for-api.herokuapp.com"],
  },
};

module.exports = nextConfig;
// "nextjs-blog-for-api.herokuapp.com"
